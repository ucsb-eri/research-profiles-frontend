# Loaded automatically by `next dev`.
# Run the backend locally on port 3001 (it defaults to 3000, which Next.js dev uses):
#   cd ../research-profiles && PORT=3001 npm start
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
